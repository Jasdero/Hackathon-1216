{% extends 'AppPhotoBundle:Default:frontLayout.html.twig' %}

{% block body %}

    <div class="container-fluid">
        <div class="row formulaire">
            <div class="col-xs-offset-2 col-xs-8 col-xs-offset-2">
                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <input type="submit" value="Modifier" />
                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
    <div class="container-fluid">
        <div class="row ">
            <div class="col-xs-offset-2 col-xs-8 col-xs-offset-2">
                <a href="{{ path('editAvatar') }}"><button>Changer mon avatar</button></a>
            </div>
        </div>
    </div>
{% endblock %}
{% block footer %}
    <div class="navbar-fixed-bottom">
        <div class="container">
            <div class="col-xs-4">
            <span>
                <a href="{{ path('index') }}"><i class="fa fa-inverse fa-chevron-left fa-3x"></i></a>
            </span>
            </div>
            <div class="col-xs-4">
                <a title="Créer une partie" href="#"><i class="fa fa-plus fa-3x" aria-hidden="true"></i>	</a>
            </div>
            <div class="col-xs-4">
                <a title="Classement" href="{{ path('classement') }}"><i class="fa fa-bar-chart fa-3x" aria-hidden="true"></i></a>
            </div>
        </div>
    </div>
{% endblock %}


{% extends 'base.html.twig' %}

{% block body %}
	<h1>Game creation</h1>

	{{ form_start(form) }}
		<div class="form-group  field-vich_image"><label class="col-sm-2 control-label">Image file</label>
			<div class="col-sm-10">
				<div class="easyadmin-vich-image">
					<input id="image_imageFile_file" name="imageFile" type="file">
				</div>
			</div>
		</div>
		<input type="submit" value="Create"/>
	{{ form_end(form) }}

	<ul>
		<li>
			<a href="{{ path('game_index') }}">Back to the list</a>
		</li>
	</ul>
{% endblock %}

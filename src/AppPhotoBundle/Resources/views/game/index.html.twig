{% extends 'base.html.twig' %}

{% block body %}
    <h1>Games list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <td>Image à trouver</td>
        </thead>
        <tbody>
        {% for game in games if (game.toGuessImage is not null) %}
            <tr>
                <td><a href="{{ path('game_show', { 'id': game.id }) }}">{{ game.id }}</a></td>
                <td>
                    <a href="{{ path('game_show', { 'id': game.id }) }}">
                        <img src="{{ asset('uploads/images/medias/' ~ game.toGuessImage.image) }}" alt="{{ game.toGuessImage.comment }}">
                    </a>
                </td>
                <td>
                    <li><a href="{{ path('game_show', { 'id': game.id }) }}">Lancez-vous !</a></li>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('game_new') }}">Create a new game</a>
        </li>
    </ul>
{% endblock %}
